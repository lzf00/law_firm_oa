# Changelog

本项目采用 [Semantic Versioning](https://semver.org/)；正式版本条目按
“新增、变更、修复、安全、迁移、已知限制”归档。开发中的变化先记录在
`Unreleased`，发布时转入带日期的版本章节。

## Unreleased

### 新增

- 利益冲突检索升级为正式准入工作台：支持预检、正式申报、独立复核、风险结论、豁免
  措施、命中快照和不可变处置轨迹；申请人不能复核本人申请。
- 合同管理增加安全版本台账、审批前病毒扫描门槛、审批联动、定稿、签署件归档、
  SHA-256 摘要和不可变生命周期记录。
- 新增利益冲突与合同生命周期细粒度权限，管理员可分别配置创建、全局查看、复核、
  管理、定稿和签署件归档能力。
- 文档中心增加当前文件详情、完整版本历史、在线预览、安全下载、新版本上传和细粒度
  人员授权管理；授权支持预览、下载、编辑、分享以及可选到期时间。
- 文档治理增加模板、标准条款和保留规则的创建、编辑、版本递增与启停界面，以及法律
  保全创建和解除流程。
- 新增文档工作台中英文桌面/移动端 Playwright 回归和授权策略单元测试。

### 安全

- 合同评审人只获得合同上下文内所需文档访问，不扩大案件其他文件权限；签署件上传
  与定稿能力分离，并对跨案件、跨办公室和无权限访问失败关闭。
- 利益冲突命中记录固化主体、案件和角色快照，复核动作及合同生命周期记录由数据库
  触发器禁止更新和删除。
- 文档授权限定同组织有效用户，授权创建与撤销写入审计日志；办公室治理管理员只能
  管理其数据范围内的文档。
- 新版本上传同时校验文档上下文和 `EDIT`/`SHARE` 权限，避免授权语义与后端写入边界
  不一致。

### 测试

- 全新空库完成 24 个迁移和冲突/合同真实 API 全链路验收，覆盖自审隔离、豁免依据、
  安全上传、审批通过/驳回、定稿、签署归档、越权及不可变触发器；后端 109 项、
  前端 12 项和中英双语/移动端浏览器 12 项测试通过。
- 隔离环境完成两版 PDF 上传与内容摘要、预览/下载、授权撤销、治理版本、保留规则、
  法律保全和审计链路 10/10 验收；后端 102 项、前端 12 项和新增浏览器 6 项测试通过。

## 0.3.0-rc.1 - 2026-07-28

### 新增

- 文档治理工作台：版本、受控预览元数据、OCR/全文索引、授权检索、模板、条款库、
  保管规则、法律保全、受阻删除和带摘要的异步导出。
- 法律财务工作台：委托与收费约定、工时和费率快照、账单审核/签发/作废、不可变账单
  行、回款分配、催收记录，以及 WIP、未开票、收入、应收账龄和利用率报表。
- 管理控制台：组织设置、用户/角色/权限、办公室及临时访问、最后管理员保护、组织同步
  预演、流程分配规则、集成健康、数据导入模板和逐行错误报告。
- 通用分页边界、100 用户代表性负载脚本、Playwright 中英双语/响应式/可访问性回归、
  CodeQL、依赖审查、SBOM、源码与镜像安全扫描。

### 变更

- 生产编排增加数据库连接池、Redis ACL/TLS、对象存储最小权限、反向代理限流和监控
  指标；前端 Nginx 改为非 root 用户并在容器内监听 8080。
- 开发编排的 PostgreSQL、Redis、MinIO、后端、前端端口及对象存储公开地址支持环境
  变量覆盖，便于干净隔离环境并行验收。
- 客户可见文案统一进入经审阅的中英文目录，法律状态使用集中格式器。
- 正式版本号、前后端制品和发布证据统一为 `0.3.0-rc.1`。

### 修复

- 修复导出完成查询在只读事务中写审计而返回 500。
- 修复跨办公室案件成员无法登记工时的重复办公室限制。
- 修复请求参数约束异常被错误映射为 500。
- 修复品牌辅助文字、装饰编号和移动滚动表格的 WCAG AA 问题。
- 修复全新数据库中会议室开发种子与办公室范围部分唯一索引不匹配而无法启动。
- 补齐开发组织后置创建时的角色和权限对账，避免干净环境管理员出现功能 403。

### 安全

- PostgreSQL JDBC 升级至 42.7.12，Bouncy Castle 固定为 1.84。
- CI 使用固定版本的 Trivy 容器直接扫描，避免依赖受影响的第三方 Action 标签。
- 最终源码、后端镜像、前端镜像和 npm 生产依赖 High/Critical 均为 0。

### 已知限制

- 正式钉钉企业应用、域名/TLS、托管数据库 PITR、生产对象版本恢复、密钥托管、电子
  签章、发票/财务提供商仍需客户资源完成环境验收。

## 0.2.0-rc.1 - 2026-07-28

### 安全

- 浏览器生产会话改为 `Secure`、`HttpOnly`、`SameSite=Lax` Cookie，登录响应不再向
  JavaScript 暴露访问令牌。
- 会话主体增加版本、用户 UUID 和组织 UUID，旧版仅用户名会话会自动失效。
- 钉钉 OAuth state、用户交换和本地身份查找均绑定明确的组织。
- 生产环境必须配置 `APP_ORGANIZATION_ID`，不再选择数据库第一条组织记录。
- 文档上传引入隔离、扫描、可用、拒绝和扫描失败状态；文件未通过服务端签名、扩展名、
  压缩包膨胀、宏和恶意样本检查前，不能预览或下载。
- 生产环境强制启用独立 ClamAV 扫描服务；扫描器不可用时失败关闭，不把未扫描对象
  暴露给用户。
- 过期上传和未引用隔离对象由定时任务分批清理，并记录成功与失败指标。

### 测试

- 新增 Cookie 属性、会话版本、两组织同名用户、同外部 ID 和跨租户拒绝测试。
- API 冒烟扩展至 43 项，覆盖主体/客户、合同、期限、案件工作台、生命周期、审批动作、
  通知与电子卷宗的新增、编辑、关联、提醒和权限策略。
- 后端单元与边界测试扩展至 82 项，新增全量英文错误目录、文件签名、压缩包/宏、
  确定性恶意样本、扫描失败和隔离对象清理回归。
- 前端单元测试扩展至 10 项，新增中英文目录键一致性、空翻译与法律术语完整性校验。
- 浏览器验收覆盖 16 个业务页面的中英文、桌面和移动端组合，并断言无页面级横向
  溢出、无英文固定文案遗漏、无 critical 级 WCAG 问题、无控制台/运行时/HTTP 5xx。

### 新增

- 客户与主体页支持双语新增/编辑主体、别名维护、客户转化和客户档案编辑。
- 合同页支持双语新增/编辑、客户/案件关联及合同评审送审。
- 期限页支持双语新增/编辑、案件与负责人选择、优先级和自定义提醒天数。
- 新增案件工作台，统一展示案件概览、当事人、团队、冲突检索、合同、期限、审批、
  文档、电子卷宗与案件动态。
- 电子卷宗支持绑定案件、查看卷内目录，并仅允许将本案件且当前用户有权访问的文档
  加入卷宗。
- 案件列表支持服务端状态筛选与搜索；案件工作台支持资料编辑、受控状态流转、原因记录
  与越权拦截。
- 审批中心支持通过、驳回、转交和催办；驳回原因服务端必填，转交候选人按业务访问范围
  过滤，同一任务 30 分钟内禁止重复催办。
- 全局通知抽屉支持未读计数、逐条已读、同源可信深链和已失效/无权限目标提示。
- 新增统一法律业务状态、角色、优先级、文件类型、保管策略和工作语言双语格式器。
- 增加跳转到主内容、全局键盘焦点、通知抽屉焦点圈定与恢复、状态播报，以及移动端
  44px 触控目标和自适应对话框。

### 修复

- 修复合同与期限页面在依赖数据尚未加载完成时提前打开表单，导致负责人默认值为空的
  竞态问题。
- 修复文档表格和英文移动端顶栏造成的页面级横向溢出。

## 0.1.0 - 2026-07-28

### 新增

- 建立律师 OA 商业化开发基线：案件、合同、文档、审批、通知、办公模块、双语和
  办公室权限。
- 发布后端、前端、API 冒烟、浏览器端到端、生产配置与备份恢复验证基线。
