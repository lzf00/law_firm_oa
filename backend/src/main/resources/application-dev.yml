app:
  auth:
    mode: dev

spring:
  flyway:
    locations: classpath:db/migration,classpath:db/dev
    out-of-order: true
  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.zoro.legaloa: DEBUG
